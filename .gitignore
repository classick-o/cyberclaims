# dependencies
node_modules/

# build output
dist/
.astro/

# uploaded media (served from disk; backed up separately, never committed)
uploads/

# env & secrets
.env
.env.*
!.env.example
.mcp.json

# logs & caches
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# working scratch files (site-scrape extract)
_home_text.txt

# Deploy credentials. deploy/ is tracked (scripts + README) but the env files hold live
# database / JWT / SMTP / Turnstile / ScamInfo secrets and this repo has a GitHub remote.
deploy/env.*
