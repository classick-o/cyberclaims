---
// Sticky header: transparent → dark on scroll (brief §4 Nav). Mega-menu for services.
import Logo from './Logo.astro';
import Icon from './Icon.astro';
import { SERVICES, URL_CHECKER } from '../data/site';
import { link } from '../lib/paths';
---

<header id="site-header" class="site-header" data-at-top="true">
  <div class="wrap header-inner">
    <Logo />

    <nav class="nav-desktop" aria-label="Primary">
      <a href={link('/')} class="nav-link">Home</a>
      <a href={link('/about-us/')} class="nav-link">About Us</a>

      <div class="nav-has-mega">
        <button class="nav-link nav-trigger" aria-expanded="false" aria-haspopup="true">
          Our Services
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="m6 9 6 6 6-6" /></svg>
        </button>

        <div class="mega" role="menu">
          <div class="mega-grid">
            {
              SERVICES.map((s) => (
                <a href={link(s.href)} class="mega-item" role="menuitem">
                  <span class="mega-icon" style={`color:${s.accent}`}>
                    <Icon name={s.icon} class="w-5 h-5" />
                  </span>
                  <span class="mega-text">
                    <span class="mega-title">{s.title}</span>
                    <span class="mega-blurb">{s.blurb}</span>
                  </span>
                </a>
              ))
            }
          </div>
          <a href={link(URL_CHECKER.href)} class="mega-foot">
            <Icon name="url" class="w-4 h-4" /> Free {URL_CHECKER.title} - verify a suspicious link
          </a>
        </div>
      </div>

      <a href={link('/news/')} class="nav-link">News</a>
      <a href={link('/contact-us/')} class="nav-link">Contact Us</a>
    </nav>

    <a href={link('/start-process/')} class="btn btn-header">Start Process</a>

    <button id="burger" class="burger" aria-label="Open menu" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </div>

  <!-- Mobile drawer -->
  <div id="mobile-drawer" class="mobile-drawer">
    <nav class="mobile-nav" aria-label="Mobile">
      <a href={link('/')}>Home</a>
      <a href={link('/about-us/')}>About Us</a>
      <details>
        <summary>Our Services</summary>
        <div class="mobile-services">
          {SERVICES.map((s) => <a href={link(s.href)}>{s.title}</a>)}
          <a href={link(URL_CHECKER.href)}>{URL_CHECKER.title}</a>
        </div>
      </details>
      <a href={link('/news/')}>News</a>
      <a href={link('/contact-us/')}>Contact Us</a>
      <a href={link('/start-process/')} class="btn mobile-cta">Start Process</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    inset: 0 0 auto 0;
    z-index: 60;
    transition: background 0.4s var(--ease-out-brand),
      box-shadow 0.4s var(--ease-out-brand), backdrop-filter 0.4s;
  }
  .site-header[data-at-top='false'] {
    background: rgba(16, 16, 32, 0.82);
    backdrop-filter: blur(14px);
    box-shadow: 0 1px 0 rgba(255, 255, 255, 0.06);
  }
  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    height: 4.75rem;
  }
  .nav-desktop {
    display: none;
    align-items: center;
    gap: 0.35rem;
    margin-left: auto;
    margin-right: 1.25rem;
  }
  .nav-link {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.5rem 0.85rem;
    font-size: 0.92rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.82);
    background: none;
    border: none;
    cursor: pointer;
    font-family: var(--font-sans);
  }
  /* underline slide-in from left (brief §5) */
  .nav-link::after {
    content: '';
    position: absolute;
    left: 0.85rem;
    right: 0.85rem;
    bottom: 0.3rem;
    height: 1.5px;
    background: var(--color-purple-300);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s var(--ease-out-brand);
  }
  .nav-link:hover {
    color: #fff;
  }
  .nav-link:hover::after {
    transform: scaleX(1);
  }

  .nav-has-mega {
    position: static;
  }
  .mega {
    position: absolute;
    left: 50%;
    top: calc(100% - 0.4rem);
    transform: translateX(-50%) translateY(10px);
    width: min(56rem, calc(100vw - 3rem));
    padding: 1.1rem;
    background: rgba(26, 26, 46, 0.96);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.75rem;
    box-shadow: 0 30px 60px -20px rgba(0, 0, 0, 0.7);
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.28s var(--ease-out-brand),
      transform 0.28s var(--ease-out-brand), visibility 0.28s;
  }
  .nav-has-mega:hover .mega,
  .nav-has-mega:focus-within .mega {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
  }
  .mega-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.35rem;
  }
  .mega-item {
    display: flex;
    gap: 0.75rem;
    padding: 0.7rem 0.8rem;
    border-radius: 0.75rem;
    text-decoration: none;
    transition: background 0.2s;
  }
  .mega-item:hover {
    background: rgba(139, 91, 189, 0.12);
  }
  .mega-icon {
    flex-shrink: 0;
    width: 2.4rem;
    height: 2.4rem;
    display: grid;
    place-items: center;
    border-radius: 0.6rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }
  .mega-text {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }
  .mega-title {
    font-size: 0.9rem;
    font-weight: 600;
    color: #fff;
  }
  .mega-blurb {
    font-size: 0.78rem;
    line-height: 1.35;
    color: rgba(255, 255, 255, 0.55);
  }
  .mega-foot {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 0.75rem;
    padding: 0.7rem 0.85rem;
    border-radius: 0.75rem;
    font-size: 0.82rem;
    font-weight: 500;
    color: var(--color-purple-200);
    background: rgba(139, 91, 189, 0.1);
    text-decoration: none;
  }
  .mega-foot:hover {
    background: rgba(139, 91, 189, 0.18);
  }
  .mega-foot :global(svg) {
    width: 1rem;
    height: 1rem;
  }

  .btn-header {
    display: none;
    padding: 0.7rem 1.4rem;
    font-size: 0.88rem;
  }

  .burger {
    display: inline-flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 2.6rem;
    height: 2.6rem;
    background: none;
    border: none;
    cursor: pointer;
    margin-left: auto;
  }
  .burger span {
    display: block;
    height: 2px;
    width: 22px;
    margin-inline: auto;
    background: #fff;
    transition: transform 0.3s, opacity 0.3s;
  }
  .burger[aria-expanded='true'] span:nth-child(1) {
    transform: translateY(7px) rotate(45deg);
  }
  .burger[aria-expanded='true'] span:nth-child(2) {
    opacity: 0;
  }
  .burger[aria-expanded='true'] span:nth-child(3) {
    transform: translateY(-7px) rotate(-45deg);
  }

  .mobile-drawer {
    overflow: hidden;
    max-height: 0;
    background: rgba(16, 16, 32, 0.98);
    backdrop-filter: blur(14px);
    transition: max-height 0.4s var(--ease-out-brand);
  }
  .mobile-drawer.open {
    max-height: 90vh;
  }
  .mobile-nav {
    display: flex;
    flex-direction: column;
    padding: 1rem 1.5rem 2rem;
    gap: 0.25rem;
  }
  .mobile-nav > a,
  .mobile-nav summary {
    padding: 0.85rem 0.25rem;
    color: rgba(255, 255, 255, 0.85);
    text-decoration: none;
    font-size: 1.05rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    cursor: pointer;
  }
  .mobile-services {
    display: flex;
    flex-direction: column;
    padding: 0.25rem 0 0.5rem 0.9rem;
  }
  .mobile-services a {
    padding: 0.6rem 0;
    font-size: 0.92rem;
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
  }
  .mobile-cta {
    margin-top: 1rem;
    justify-content: center;
  }

  @media (min-width: 1024px) {
    .nav-desktop {
      display: flex;
    }
    .btn-header {
      display: inline-flex;
    }
    .burger {
      display: none;
    }
  }
</style>

<script>
  const header = document.getElementById('site-header')!;
  const onScroll = () =>
    header.setAttribute('data-at-top', String(window.scrollY < 24));
  onScroll();
  window.addEventListener('scroll', onScroll, { passive: true });

  const burger = document.getElementById('burger')!;
  const drawer = document.getElementById('mobile-drawer')!;
  burger.addEventListener('click', () => {
    const open = drawer.classList.toggle('open');
    burger.setAttribute('aria-expanded', String(open));
  });
  drawer.querySelectorAll('a').forEach((a) =>
    a.addEventListener('click', () => {
      drawer.classList.remove('open');
      burger.setAttribute('aria-expanded', 'false');
    })
  );
</script>
