---
// Cyberclaims mark - animated ring (upgrade of the spinning-logo GIF to CSS, brief §2.2).
import { link } from '../lib/paths';
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<a href={link('/')} class={`logo ${className}`} aria-label="Cyberclaims home">
  <span class="logo-mark" aria-hidden="true">
    <img src={link('/favicon-192.png')} alt="" width="40" height="40" />
  </span>
  <span class="logo-word">Cyber<span class="logo-word-accent">claims</span></span>
</a>

<style>
  .logo {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
  }
  .logo-mark {
    width: 2.25rem;
    height: 2.25rem;
    display: grid;
    place-items: center;
  }
  .logo-mark img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }
  .logo-word {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.2rem;
    letter-spacing: -0.02em;
    color: #fff;
  }
  .logo-word-accent {
    color: var(--color-purple-300);
  }
</style>
