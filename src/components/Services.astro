---
// Services preview: horizontally scrollable glass cards with hover lift + accent line (brief §4/§5).
import Icon from './Icon.astro';
import { SERVICES } from '../data/site';
import { link } from '../lib/paths';
---

<section class="services">
  <div class="wrap services-head">
    <div data-reveal>
      <h2 class="h-section">What we <span class="accent">investigate</span></h2>
    </div>
    <div class="services-nav" data-reveal>
      <button class="rail-btn rail-prev" data-dir="-1" aria-label="Previous" disabled><Icon name="arrow" class="w-5 h-5" /></button>
      <button class="rail-btn rail-next" data-dir="1" aria-label="Next"><Icon name="arrow" class="w-5 h-5" /></button>
    </div>
  </div>

  <div class="rail-viewport">
    <div class="rail no-scrollbar" id="services-rail" data-reveal>
      {
        SERVICES.map((s) => (
          <a href={link(s.href)} class="svc-card" style={`--accent:${s.accent}`}>
            <span class="svc-watermark" aria-hidden="true"><Icon name={s.icon} class="w-full h-full" /></span>
            <span class="svc-content">
              <h3 class="svc-title">{s.title}</h3>
              <p class="svc-blurb">{s.blurb}</p>
              <span class="svc-more">Read more <Icon name="arrow" class="w-4 h-4" /></span>
            </span>
            <span class="svc-line"></span>
          </a>
        ))
      }
    </div>
  </div>
</section>

<style>
  .services {
    position: relative;
    padding: 3rem 0;
    overflow: hidden;
  }
  .services-head,
  .rail-viewport {
    position: relative;
    z-index: 1;
  }
  .services-head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }
  .services-head h2 {
    margin-top: 0.9rem;
  }
  .services-nav {
    display: none;
    gap: 0.6rem;
  }
  .rail-btn {
    width: 2.8rem;
    height: 2.8rem;
    border-radius: 50%;
    display: grid;
    place-items: center;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: #fff;
    cursor: pointer;
    transition: background 0.2s, border-color 0.2s;
  }
  .rail-btn:hover {
    background: var(--color-purple-500);
    border-color: var(--color-purple-400);
  }
  /* left button points left */
  .rail-prev :global(svg) {
    transform: rotate(180deg);
  }
  /* disabled state (e.g. prev at start, next at end) */
  .rail-btn:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }
  .rail-btn:disabled:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.12);
  }

  .rail-viewport {
    max-width: 80rem;
    margin-inline: auto;
    padding-inline: var(--spacing-gutter);
  }
  .rail {
    display: flex;
    gap: 1.25rem;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding: 0.5rem 0 1.5rem;
    scroll-padding-left: 0;
  }
  .svc-card {
    position: relative;
    display: flex;
    flex-direction: column;
    flex: 0 0 auto;
    width: 18rem;
    padding: 1.25rem;
    scroll-snap-align: start;
    background: rgba(35, 35, 64, 0.5);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 0.75rem;
    text-decoration: none;
    overflow: hidden;
    transition: transform 0.35s var(--ease-out-brand),
      box-shadow 0.35s var(--ease-out-brand), border-color 0.35s;
  }
  .svc-card:hover {
    transform: translateY(-8px);
    border-color: color-mix(in srgb, var(--accent) 50%, transparent);
    box-shadow: 0 24px 48px -20px rgba(0, 0, 0, 0.6),
      0 0 40px -12px color-mix(in srgb, var(--accent) 45%, transparent);
  }
  /* large faded watermark icon centered in card background */
  .svc-watermark {
    position: absolute;
    top: 42%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 11rem;
    height: 11rem;
    color: var(--accent);
    opacity: 0.1;
    pointer-events: none;
    z-index: 0;
    transition: opacity 0.35s var(--ease-out-brand),
      transform 0.5s var(--ease-out-brand);
  }
  .svc-card:hover .svc-watermark {
    opacity: 0.16;
    transform: translate(-50%, -50%) scale(1.06);
  }
  .svc-content {
    position: relative;
    z-index: 1;
    flex: 1;
    display: flex;
    flex-direction: column;
  }
  .svc-title {
    font-size: 1.15rem;
    color: #fff;
    margin-bottom: 0.6rem;
  }
  .svc-blurb {
    font-size: 0.88rem;
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.58);
    margin-bottom: 1.1rem;
  }
  .svc-more {
    align-self: flex-start;
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-display);
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--color-purple-300);
  }
  .svc-more :global(svg) {
    transition: transform 0.3s var(--ease-out-brand);
  }
  .svc-card:hover .svc-more :global(svg) {
    transform: translateX(4px);
  }
  /* accent line appears at bottom on hover (brief §5) */
  .svc-line {
    position: absolute;
    left: 0;
    bottom: 0;
    height: 3px;
    width: 100%;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s var(--ease-out-brand);
  }
  .svc-card:hover .svc-line {
    transform: scaleX(1);
  }

  @media (min-width: 768px) {
    .services-nav {
      display: flex;
    }
  }
</style>

<script>
  const rail = document.getElementById('services-rail');
  const prev = document.querySelector<HTMLButtonElement>('.rail-prev');
  const next = document.querySelector<HTMLButtonElement>('.rail-next');

  // Enable/disable arrows based on scroll position.
  function updateNav() {
    if (!rail || !prev || !next) return;
    const atStart = rail.scrollLeft <= 2;
    const atEnd = rail.scrollLeft + rail.clientWidth >= rail.scrollWidth - 2;
    prev.disabled = atStart;
    next.disabled = atEnd;
  }

  [prev, next].forEach((btn) =>
    btn?.addEventListener('click', () => {
      const dir = Number(btn.dataset.dir) || 1;
      // scroll by ~one card width so paging feels natural
      rail?.scrollBy({ left: dir * 320, behavior: 'smooth' });
    })
  );

  rail?.addEventListener('scroll', updateNav, { passive: true });
  window.addEventListener('resize', updateNav);
  updateNav();
</script>
